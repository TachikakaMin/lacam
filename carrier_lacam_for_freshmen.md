# Carrier-LaCAM：让机器人和货架在同一个搜索里行动

> 本文面向学过数组、图和基本搜索，但还没有接触过多智能体路径规划的大一 CS 学生。内容以仓库中的实际 C++ 实现为准，不把论文术语当作解释。文中的公式全部使用终端可读的纯文本，不使用 LaTeX。

## 1. 先别急着看算法：把一个小仓库搬完

先看一个“单通道加侧袋”的小仓库。机器人在地面层移动，货架在上层占格；两层使用同一套坐标和墙，但机器人足够矮，所以空手时可以钻到落地货架下面。

```text
        col 0  col 1  col 2
row 0     A/R      B      G
row 1      #       .      #

R   = 机器人
A   = 有任务的目标货架
B   = 没有身份的普通货架
G   = A 的目标位置
A/R = R 位于 A 正下方
.   = 可以暂存 B 的侧袋
#   = 墙
```

A 从 `(0,0)` 到 G 只有一条上层通道：`(0,0) -> (0,1) -> (0,2)`，可是 `(0,1)` 上放着 B。机器人不能举着 A 穿过 B，因为那会让两个货架在上层占据同一个格子；唯一的侧袋 `(1,1)` 正好可以临时存放 B。一个可执行计划是：

```text
1.  R 从 (0,0) 移到 (0,1)，钻到 B 正下方
2.  R 执行 LIFT，举起 B
3.  R 带着 B 移到侧袋 (1,1)
4.  R 执行 DROP，把 B 临时放在 (1,1)
5.  R 从 (1,1) 移回通道 (0,1)
6.  R 从 (0,1) 移到 (0,0)，回到 A 正下方
7.  R 执行 LIFT，举起 A
8.  R 带着 A 移到 (0,1)
9.  R 带着 A 移到目标 (0,2)
10. R 执行 DROP，把 A 放到 G
```

这里已经出现了 DD-MAPD 最重要的三个特点。第一，空手机器人和落地货架可以位于同一坐标，因为它们在不同层。第二，举起货架后，机器人和货架会一起移动，此时上层是否有空间也必须检查。第三，B 没有自己的目标，却必须先被搬开；因此“给 A 找一条路”和“给机器人找一条路”不能真的分开。

Carrier-LaCAM 的核心选择是：**不先规划一条想象中的货架轨迹，再祈祷机器人能够执行；它直接搜索整个仓库的物理状态。货架没有独立动作，货架移动永远是机器人动作的结果。**

## 2. 一个时间步到底怎样发生

系统采用离散、同步时间。所有机器人都读取同一个时刻 `t` 的状态，各自选择一个动作，然后验证器一次性判断这组联合动作是否合法，最后统一生成时刻 `t+1`：

```text
state[t]
  -> 所有机器人同时选择动作
  -> apply_ops 检查整组动作
  -> state[t+1]
```

“同时”非常重要。假设机器人 A 离开格子 2，机器人 B 同一步进入格子 2，只要最终没有重叠，而且两者不是迎面交换位置，这种 following 是允许的。相反，不能先在脑中执行 A，再执行 B，因为那会偷偷把同步问题改成顺序问题。

每个机器人每步只能选择四类原子动作：

| 动作 | 机器人位置 | 搬运关系 | 货架位置 |
| --- | --- | --- | --- |
| `WAIT` | 不变 | 不变 | 不变 |
| `MOVE(to)` | 移到相邻非墙格 | 不变 | 若正搬着货架，货架一起到 `to` |
| `LIFT` | 不变 | 从空闲变为搬运脚下货架 | 货架仍在当前格，只是由落地变成被举起 |
| `DROP` | 不变 | 从搬运变为空闲 | 货架仍在当前格，只是由被举起变成落地 |

`LIFT` 和 `DROP` 都消耗一个完整时间步。`WAIT`、`LIFT`、`DROP` 的目的格都是机器人当前格，所以只记录“下一步在哪”无法区分它们。这正是 Carrier-LaCAM 把低层约束从“目的格”升级成“完整动作算子”的原因。

## 3. 搜索状态保存了什么

实现中的物理状态可以用下面这个纯文本四元组理解：

```text
X = (robot_pos, target_pos, anon_occ, carrying)
```

`robot_pos[i]` 是第 `i` 个机器人的位置。机器人有身份，因为不同机器人的搬运状态和未来动作可能不同。`target_pos[b]` 是第 `b` 个目标货架的位置；即使货架正被举着，这个位置也等于载具机器人的位置。目标货架必须保留身份，因为 A 和 B 的终点可能不同。

`anon_occ` 是所有**落地匿名货架**占据的格子集合，在实现中排序后保存。匿名货架没有各自的终点、重量、尺寸或不同动作规则，所以交换两个匿名货架的编号不会改变未来可执行性；只保存“哪些格子上有匿名货架”就够了。匿名货架一旦被举起，会暂时从 `anon_occ` 中消失。

`carrying[i]`，源码中叫 `kappa[i]`，表示机器人 `i` 正在搬什么：

```text
-1       表示空闲
-2       表示搬着某个匿名货架
0,1,2... 表示搬着对应编号的目标货架
```

因此，完整搜索键不是“机器人位置向量”，而是：

```text
SearchKey = (所有机器人位置, 所有目标货架位置,
             所有落地匿名货架位置, 每个机器人的搬运关系)
```

两个节点即使机器人站位完全相同，只要货架位置或搬运关系不同，就是两个不同物理状态。反过来，guidance 给出的请求、匹配和停车建议不属于物理状态，也不进入去重键；它们只是“接下来先试什么”的建议。

终点条件也很严格：每个目标货架都必须位于自己的目标格，并且已经落地。一个机器人举着 A 悬停在 A 的目标上方，还不算完成，因为它至少还要执行一次 `DROP`。

## 4. `apply_ops`：每条搜索边的物理裁判

给定当前状态 X 和每个机器人的一个动作，`apply_ops` 要么返回唯一的下一状态，要么拒绝这组动作。它是最终裁判，候选生成器中的快速检查不能替代它。

验证大致分成三步。第一步根据时刻 `t` 的快照检查每个动作的前置条件并计算暂定结果：`MOVE` 必须走到四邻接非墙格；空闲机器人才能 `LIFT`；`LIFT` 时脚下必须确实有一个落地货架；只有正在搬货架的机器人才能 `DROP`。目标货架被搬运时，它的位置跟着机器人更新；匿名货架被举起时从落地集合删除，放下时重新加入。

第二步检查地面层机器人冲突：

```text
R1 顶点冲突：两个机器人不能在 t+1 占据同一格。
R2 交换冲突：A 从 u 到 v、B 同时从 v 到 u，不合法。
following：A 离开 v、B 同时进入 v，只要不是交换，则合法。
```

第三步检查上层货架冲突。所有目标货架、落地匿名货架和被搬运匿名货架在 `t+1` 的格子必须互不重复。空手机器人在货架下方不构成冲突，但载具机器人想进入一个已有货架的格子时，会因为它携带的货架在上层相撞而被拒绝。

可以把判断规则记成下面这张图：

```text
地面层：墙 + 机器人最终位置唯一 + 禁止两机器人交换
上层：所有货架最终位置唯一
跨层：只有 carrying 把机器人移动和货架移动绑定起来
```

验证通过后，匿名货架占用集合重新排序，得到规范化的下一状态。这个规范化很重要：同一个匿名货架布局必须只有一种状态表示，否则去重表会把等价状态误当成不同状态。

示例中的机器人可以空手走进 B 所在的 `(0,1)`，因为地面层没有别的机器人；但它举起 B 后不能直接带着 B 走进 A 所在的 `(0,0)`，因为上层的 A 和 B 会冲突。每一条输出计划都可以逐步交给同一个 `apply_ops` 重放，因此搜索中的一条边不是“希望能执行的计划”，而是已经通过物理规则的状态转移。

## 5. 为什么不能直接枚举所有联合动作

在四邻接网格中，一个机器人最多有四个 `MOVE`、一个 `WAIT`，再加当前可用的 `LIFT` 或 `DROP`，所以局部候选最多约六个。注意，这是**一个机器人**的候选数。若有 R 个机器人，联合动作数量最坏接近：

```text
joint_actions ~= 6^R
```

20 个机器人时，`6^20` 已经约等于 `3.66e15`。真实仓库有几十、几百甚至上千机器人，不可能每个时间步先生成完整笛卡尔积，再逐一验证。

LaCAM 的办法不是放弃完整枚举，而是把枚举做成**惰性的**：先用 PIBT 很快猜一个看起来不错的联合动作；只有猜得不好或发生冲突时，才逐渐添加约束，逼迫生成器尝试别的组合。这样常见情况下很快得到后继，困难情况下又保留最终枚举所有组合的能力。

## 6. 最容易混淆的地方：外层状态图和节点内部约束树

Carrier-LaCAM 同时使用两种搜索结构，但它们解决的是不同问题：

```text
外层物理状态图：负责规划很多时间步

X0  ->  X1  ->  X2  ->  ...  ->  Xgoal
      一步       一步

每个 Xi 内部的动作约束树：只负责生成 Xi 的一步后继

不固定任何动作
  -> 固定机器人 i0 的动作
    -> 固定机器人 i1 的动作
      -> ...
        -> 固定全部 R 个机器人的动作
```

外层节点 `Xi` 是完整仓库状态。外层边表示所有机器人共同执行一个同步时间步。内层约束树节点不是新的仓库状态，它只记录：“为了生成 `Xi` 的下一步，我已经强制哪些机器人执行哪些动作。”

假设只有机器人 A 和 B。约束树根节点不固定任何动作，算法调用 PIBT，让它为 A、B 补全动作并尽快提出一个后继。与此同时，根节点会按 A 的完整候选集合生成孩子，例如：

```text
根：无约束
  |- A = WAIT
  |- A = MOVE(left)
  |- A = MOVE(right)
  |- A = LIFT
  `- ...
```

之后取出 `A = WAIT` 这个部分约束时，PIBT 只需要给 B 补动作；该约束节点又会生成“再固定 B”的孩子。树深达到 2 时，A 和 B 都已固定，形成一个完整联合动作。R 个机器人时同理，只是深度变成 R。

约束树使用 FIFO 队列逐步展开。每个外层物理节点保存自己的队列，因此这棵树可以暂停：算法先尝试一个很有希望的物理后继，之后若又回到该节点，还能从上次的位置继续枚举。约束变量的顺序 `constraint_order` 在节点创建时冻结；活锁处理可以改变 PIBT 的偏好顺序，却不能改变这棵穷举树的变量顺序。

这里还要强调“完整动作”的必要性。若机器人留在原格，仅靠下一位置无法判断它执行了 `WAIT`、`LIFT` 还是 `DROP`。约束树的分支必须保存 `(目的格, 动作类型)`，否则三个物理结果会被错误合并。

约束树叶子本身也不是新的仓库节点。叶子只提供一个完整联合动作；`apply_ops` 先据此算出物理状态值 `Xnext`，然后搜索查询 CLOSED。若 `Xnext` 从未出现，才创建一个新的外层 `TAPFNode`；若它已经出现，就只记录一条指向旧节点的边，并尝试 g 松弛和 rewire。源码中的内层记录类型是 `TAPFConstraint`；PIBT 内部的递归调用既不是 `TAPFNode`，也不是 `TAPFConstraint`，它只是为当前这一个时间步填写尚未确定的动作。

## 7. PIBT：先快速拼出一个联合动作

约束树负责不漏，PIBT 负责让算法通常不必走到穷举树的深处。PIBT 可以理解成“按优先级预约下一时刻的格子，遇到人就递归请对方让路”。

看一条直线：

```text
当前时刻 t：

cell 1  cell 2  cell 3  cell 4
   A       B       C       .

偏好：
A 想去 2，B 想去 3，C 想去 4
```

若 A 优先级最高，递归过程是：

```text
1. A 预约 t+1 的格子 2。
2. 格子 2 在 t 时刻由 B 占据，于是递归处理 B。
3. B 预约 t+1 的格子 3，并递归处理 C。
4. C 预约空格 4，成功。
5. 递归逐层返回，得到 A:1->2、B:2->3、C:3->4。
```

这些预约针对的是 `t+1`，不是当前占用。算法同时检查交换：如果 A 预约 B 的当前位置，而 B 又想回到 A 的当前位置，这个两两交换会被拒绝。链式 following 则允许，所以三个机器人可以像队列前移一样在同一步移动。

载具让递归失败变得更常见。B 在地面层也许能让到某个格子，但若 B 正举着货架，那个格子的上层可能已被另一个货架占用。若 C 最终无处可去，A 这次“推人”就失败。Carrier-LaCAM 对载具角色会撤销这次尝试留下的机器人预约和上层货架预约，然后让 A 尝试下一个候选：

```text
尝试候选 u
  -> 临时预约 u
  -> 递归推动占用者
  -> 若失败：释放机器人预约和货架预约
  -> 尝试下一个候选
```

这是一个重要的集成修复。上游任务机器人原本保留失败推动的占位，这对普通 MAPF 是既有的剪枝语义；直接照搬到载具后，一次上层冲突会污染本步剩余候选。当前实现按角色分派语义：普通任务机器人保持上游行为，载具失败则释放并重试；载具只要原地等待可行，等待就算成功。

PIBT 仍然只是一个快速补全器。它可能因为贪心顺序而失败，也可能先提出很差的动作，但这不会破坏完备性：当前外层节点的约束树还在，之后会固定更多动作继续尝试。

## 8. G1：让快速生成器可以犯错，但不能漏掉合法一步

部分约束还没有固定全部机器人时，刚刚介绍的 PIBT 生成器会做很多便宜的内联检查，例如下一个机器人格是否已被预约、载具目标格的上层是否已被占用。这些检查能尽早拒绝明显失败的分支，但复杂的同步规则很容易让局部检查过于保守。

G1 规则解决了这个风险：

> 当动作约束树深度达到 R，即所有机器人的动作都已明确时，跳过生成器的内联否决，让 `apply_ops` 成为唯一裁判。

这个规则允许“快速层”和“正确性层”职责分离。内联规则写得准，搜索会快；内联规则偶尔错杀一个部分约束，完整联合动作仍会在树叶重新出现，并由权威验证器判断。实现中的 G1 对照测试会穷举小状态的所有动作组合，比较约束树最终枚举出的合法后继与 `apply_ops` 直接得到的合法后继集合。

现在可以严谨地区分两层完备性。

**局部结论：一个状态的合法一步后继不会漏。** 原因是每个机器人完整的原子动作集合都在约束树中；树最终枚举这些集合的笛卡尔积；完整叶子不受保守内联检查阻挡；每个叶子最后由 `apply_ops` 接受或拒绝。因此，对固定状态 X，所有合法的 `X -> Xnext` 都能被产生。

**全局结论：若存在一条有限可行解，外层搜索最终能找到它。** 这里不能只说“一步不漏”，还要补齐外层的有限工作论证：

```text
1. 网格、机器人和货架数量有限，所以规范化后的物理状态空间有限。
2. 每个物理节点的动作约束树有限，因为机器人和每个机器人的候选数都有限。
3. 一个约束从 FIFO 队列取出后不会放回；它只生成深度更大的有限个孩子。
4. 一个完整物理状态通过 CLOSED 最多创建一次；重复命中只增加边或改进 g。
5. 找到首解之前没有 incumbent，因此不存在 f >= U 的上界剪枝。
```

在 LaCAM 标准的无界、穷尽式展开前提下，这些有限队列最终都会被处理。结合“一步后继不漏”，若某条有限解路径存在，路径上的下一状态会逐层生成，最终到达目标。首解之后的 `f >= U` 剪枝只用于寻找比已有计划更便宜的计划；此时可行解已经存在，不会撤销可行性结论。

这叫“可行性完备”，不等于“10 秒内保证有解”，也不等于“最终一定证明最优”。实际运行有 deadline，FOCAL 使用权重，项目明确只主张完备性和 anytime 改进，不主张最优收敛。

## 9. Guidance：先判断仓库现在最需要谁做什么

如果只让 PIBT 看局部距离，它不知道哪个货架挡住了哪个目标。Carrier guidance 在每个物理节点上构造一层导航信息，回答三个问题：每个未完成目标货架大致想往哪里走？当前最值得处理哪些货架？哪些空闲机器人去处理它们最便宜？

### 9.1 最少阻挡路径

对每个活跃的未完成目标货架，算法在上层网格运行 Dijkstra。墙仍然不可通过，但货架占用格不是绝对障碍，因为它将来可以被搬开。进入一个格子的代价是：

```text
空格代价       = 1
有货架格代价   = 1 + blocking_penalty
默认 blocking_penalty = 8
所以默认有货架格代价 = 9
```

这不是目标货架此刻可以直接执行的轨迹，而是一条“综合路程和清障数量后，看起来最便宜的走廊”。一条长一点但完全空的路，可能优于一条短却压着很多货架的路。

路径约定为：

```text
path[0] = 目标货架当前位置
path[1] = 希望进入的下一格
...
path[last] = 目标格
```

在开头的例子中，A 的路径是：

```text
(0,0) -> (0,1) -> (0,2)
```

`path[1]` 上有 B，所以 A 不能直接前进。guidance 不会假装 A 能穿过 B，而是生成“先清走 B”的请求。

路径按目标货架缓存。若货架刚好沿缓存路径前进一步，可以删除旧的路径头继续使用；默认的惰性失效策略主要检查“原来空的路径格是否新出现了货架”，发现后才重算。货架从路径上消失通常不强制重算，因为旧路径仍可走，只是不一定是最新最优。还有一个严格模式会比较完整占用快照。旧路径格只作为同成本路径的微小 tie-break，不能压过真实的路径代价。

### 9.2 `serve` 和 `clear` 请求

请求分两类：

```text
serve(cell)：请一个空闲机器人来到目标货架脚下并举起它
clear(cell)：请一个空闲机器人举起挡路货架并搬开
```

若目标货架尚未被举起，且 `path[1]` 没有落地货架，就为目标货架当前位置发出 `serve`。若路径上存在其他落地货架，则沿路径扫描，最多为前三个阻挡者发出 `clear`。默认优先级是：

```text
serve = 100
第一个 clear = 50
第二个 clear = 49
第三个 clear = 48
```

因此小例子一开始会对 B 所在的 `(0,1)` 发出 `clear(B)`。机器人执行这项任务、把 B 放到侧袋 `(1,1)` 后，A 的下一格变空，下一状态就会出现 `serve(A)`。请求对象只属于当前节点，不会跨状态持久保存；如果阻挡仍存在，下一物理状态会重新生成一个等价请求。实现也不会对相同的 `clear(cell)` 去重，多条目标路径可能同时请求同一格，但机器人预约和最终物理验证会阻止两个机器人在同一步重复举起同一货架。

### 9.3 用匈牙利算法分配空闲机器人

正在搬货架的机器人已有职责，不参加请求匹配。对空闲机器人和请求，算法构造费用：

```text
match_cost(robot i, request j)
    = 地面层最短距离(i, j)
      - 保持上一步同一配对的迟滞奖励
```

默认迟滞奖励 `eta = 2`。它不是让旧配对永远不变，只是在两个方案距离接近时，避免两台机器人每一步互换目标。规模允许时复用 LaCAM-TAPF 的匈牙利算法，寻找总距离较小的一对一匹配；请求多于空闲机器人时，先按请求优先级选出要匹配的部分。规模过大或关闭匈牙利开关时，使用按优先级逐个取最近机器人的贪心版本。

机器人多于请求时，未匹配机器人保持空闲角色；请求多于机器人时，一部分当前请求不会获得机器人，若对应阻挡到下一物理状态仍存在，guidance 会重新生成等价请求。匹配结果源码中叫 `rho`，它只是本节点的工作建议，不写入物理状态。

### 9.4 不同角色怎样排列动作候选

guidance 构造完成后，PIBT 按角色分层，先处理上层任务最紧迫的机器人：

| 顺序 | 角色 | 首选行为 |
| --- | --- | --- |
| 1 | 举着未寄存目标货架 | 沿该货架的最少阻挡路径前进；到目标后优先 `DROP` |
| 2 | 举着匿名货架或寄存中的目标货架 | 前往停车格，到了就优先 `DROP` |
| 3 | 已匹配请求的空闲机器人 | 前往请求格；到达且脚下有货架时优先 `LIFT` |
| 4 | 未匹配空闲机器人 | 通常先 `WAIT`；若正占着活跃通道，则优先走开 |

举着正常目标货架时，路径下一格排在普通“朝目标变近”的邻格之前；若路径头被落地货架结构性堵住，临时 `DROP` 会提前，给清障留下空间。举着待清货架时，候选按到停车格的地面距离排序。空闲机器人在货架下面移动不受上层占用限制，所以前往请求格时只需要处理机器人冲突。

角色内部通常按剩余目标距离和稳定编号排序。发生活锁时可以在角色层内洗牌，但“目标载具优先于空闲机器人”的大层次仍保留。

## 10. 为什么这些 guidance 不会偷偷删掉答案

“最多看三个阻挡者”“只激活一部分目标”“给机器人做一次匹配”听起来都像硬限制。理解 Carrier-LaCAM 正确性的关键，是看清它有两条后继生成路径：

```text
快速路径：
最少阻挡路径
  -> serve / clear 请求
  -> 请求匹配
  -> 角色候选排序
  -> PIBT 快速补全一个联合动作

穷举兜底：
当前节点的动作约束树继续展开
  -> 逐个固定每个机器人的完整原子动作
  -> 完整联合动作交给 apply_ops
```

快速路径决定“先试哪个后继”，兜底路径决定“最终会不会漏后继”。动作约束树生成每个机器人的通用候选时，仍然包含所有相邻 `MOVE`、`WAIT` 以及当前可用的 `LIFT` 或 `DROP`；它不要求动作符合某个请求、匹配或停车建议。树深达到 R 后，G1 又让权威验证器独立裁决。

因此，下列机制都可以改变搜索速度，却不会在默认 Carrier-LaCAM 中删除合法原子动作：

```text
活跃目标数量上限
最多三个 clear 请求
匈牙利匹配结果
路径缓存
停车建议
迎面让行
活锁禁忌配对
PIBT 优先级洗牌
macro 额外后继
```

实验中的 B1“冻结货架计划”消融是例外：它故意把路径下一格变成硬约束，用来模拟先规划货架再执行的分解方法。B1 本来就是弱对照，不应和默认算法的 ordering-only guidance 混为一谈。

## 11. 仅有导航还不够：跨层死锁与活锁处理

### 11.1 目标到家了，也可能暂时不能落地

假设 A 的目标格恰好在 B 当前的活跃路径上。若 A 立刻落地，B 就必须再派机器人清走 A。算法定义一个临时寄存关系：

```text
A -> B

含义：A 暂时不要在自己的目标格落地，
因为 A 的目标格正挡着 B 的活跃路径。
```

如果 A 已被举到目标上方，载具先不 `DROP`，而是按寄存角色寻找停车位。为了避免“A 悬停在自己的目标上，于是路径规划又把 A 自己视作永远的障碍”这种循环，计算路径时会把**正被搬运且悬停在自己目标上的那个目标货架**临时视为空；已经落地的目标货架仍是真实阻挡者，不能被掩掉。

寄存关系可能成环：

```text
A -> B -> C -> A
```

如果三者都坚持等待，就没有人先落位。实现沿寄存 owner 链检测环，并确定性地让环中编号最小的目标取消寄存。它先打破对称性，让至少一个成员继续推进；这只是候选排序建议，兜底树仍保留其他物理动作。

### 11.2 两个载具迎面相撞

若载具 A 的路径下一格是 B 当前格，同时 B 的路径下一格又是 A 当前格，它们想做一个上层和下层都不允许的交换。算法比较两者离目标的剩余距离，让更远的那个进入寄存/让行状态；距离相同时用稳定编号打破平局。让行者去停车，另一方先通过。

### 11.3 wait-for 图找跨层等待环

更一般的等待关系同时来自两层。算法为每个机器人最多建立一条“我正在等谁”的有向边：

```text
地面阻塞：
机器人 i 想去的下一格当前站着机器人 j
=> i -> j

上层阻塞：
载具 i 的下一格压着落地货架，
而清走该货架的请求已分配给机器人 j
=> i -> j
```

这样得到一个每个点出度至多为 1 的 wait-for 图。若图中出现 `i -> j -> k -> i`，说明当前建议形成结构性等待环。算法优先禁忌环中那些拥有请求配对的机器人，让下一次匹配换人或换请求；如果环中没有可禁忌的请求配对，才退化为禁忌全部当前配对。

### 11.4 两种活锁信号

死锁是完全走不动，活锁则是一直在动却没有进展。实现使用两个信号。

第一个信号是 `progress_score`。它把每个未完成目标到终点的墙上最短距离相加，并为**每个未完成目标固定加 2**；即使目标已经被举起，这里仍然加 2。它只是粗略的停滞信号，不是剩余举放成本，更不要求可采纳。若连续一个窗口都没有刷新历史最好值，就触发角色内优先级洗牌和禁忌配对；若 wait-for 图有环，优先只禁忌环上拥有配对的成员。

第二个信号是物理状态重访次数。同一个配置被反复命中，说明当前 guidance 可能把搜索带回熟悉的循环。达到固定重访间隔后，算法重建 guidance、禁忌当前匹配，并允许重新尝试新的优先后继。

这两类处理仍然只改变尝试顺序。即使活锁诊断不准确，动作约束树也不会因此失去分支。

## 12. 停车格为什么故意保持简单

举着匿名阻挡货架或寄存目标货架的机器人，需要找一个临时落点。算法先把活跃目标路径和所有目标格标记为保护区域，然后从载具当前位置做 BFS，优先选择最近的**其他格子**，要求其上层为空且不在保护区域；停车 BFS 本身不会把起点选作新停车格。如果找不到，再退而选择一个上层为空、不是目标格的保护区格子。PIBT 的普通动作候选中仍然可以出现原地 `DROP`，两者不要混淆。

项目曾尝试更“聪明”的评分，例如远离活跃通道、避开窄走廊、偏爱逃生出口多的格子。密集仓库中，这些规则反而会失败：墙角和窄走廊往往正是仅剩的临时腾挪空间，把它们一律判差等于主动丢掉可行缓冲区。因此生产默认值保留“最近可用空格”这一简单策略，复杂逃生度 tie-break 默认关闭。

这个教训很普遍：稀疏地图上的直觉不一定适用于高填充率重排问题。停车评价不是越复杂越好，必须由密集实例验证。

## 13. 搜索怎样评价“便宜”和“有希望”

### 13.1 真实累计代价 `g`

对纯载具问题，一个同步时间步的物理动作成本是：

```text
step_cost =
    alpha * 举着货架的 MOVE 数
  + beta  * 空闲机器人的 MOVE 数
  + gamma * (LIFT 数 + DROP 数)
  + delta * 举着匿名货架的 MOVE 数
```

其中匿名载具移动同时支付 `alpha + delta`。`WAIT` 成本为 0，但它仍会增加计划的时间步数。整条路径的：

```text
g = 所有 step_cost 的总和
```

因此优化目标主要是物理动作总成本，不等同于 makespan。makespan 是从开始到结束的同步时间步数；一个等待很多但移动很少的计划，可能动作成本不高，却耗时较长。实验会同时报告这些指标，不能把两者混写。

默认 `alpha`、`beta`、`gamma`、`delta` 都是 1，也支持显式启用加权成本。普通 TAPF 任务机器人保留原来的任务成本；没有任务的载具机器人由上述物理项计费。

### 13.2 可采纳下界 `lower_bound_h`

为了安全剪枝，需要一个绝不高估剩余真实成本的下界。对每个未完成目标货架 b，计算忽略其他货架和机器人的墙上最短距离，再加不可避免的举放次数：

```text
handling_cost(b) =
    2 * gamma    若 b 未完成且当前落地，需要 LIFT 和 DROP
    1 * gamma    若 b 未完成且正在被搬运，只需要最终 DROP
    0            若 b 已在目标格落地

lower_bound_h =
    对所有目标 b 求和：
    alpha * wall_distance(position[b], goal[b])
    + handling_cost(b)
```

它忽略了机器人走到货架下面的成本、清理匿名货架的成本、拥堵、让行和绕路，所以通常偏小，但不会因为这些被忽略的困难而高估。这个可采纳性结论针对纯 DD carrier 目标，并要求所有动作权重非负；默认实验权重全为 1。代码可以从环境变量读取自定义权重，却不会主动拒绝负数，使用负权重会破坏这套下界和上界剪枝的前提。注意它和前面的 `progress_score` 完全不同：

```text
progress_score 只负责发现“最近没进展”，不用于剪枝。
lower_bound_h  用于 f = g + h，必须不高估。
```

节点评价为：

```text
f = g + lower_bound_h
```

### 13.3 FOCAL 与 anytime 改进

没有首解也没有外部上界时，当前实现按 LaCAM 风格做 DFS，尽快深入找到一个可行解。一旦已有解或第二阶段带着上界进入，就使用加权 FOCAL。先找所有可用节点中的最小 `f_min`，再允许：

```text
f <= focal_weight * f_min
```

默认 `focal_weight = 1.5`。在这个集合里，默认优先较小的 h，再用 f、g、深度等稳定规则打破平局。这样不必永远死守最小 f，可以更快推进看起来接近完成的状态。

### 13.4 重复状态与 rewire

搜索再次生成一个已在 CLOSED 中的完整物理状态时，不创建第二份状态节点，而是记录一条邻接边。如果新路径能以更小的 g 到达旧节点，就执行类似 LaCAM* 的松弛：

```text
new_g = predecessor.g + edge_cost

若 new_g < old_node.g：
  old_node.g = new_g
  old_node.parent = predecessor
  继续向 old_node 的后继传播改进
```

这就是 rewire。它不会把同一个物理状态复制很多份，又能让后来发现的便宜路径改写最终方案。父边变化只影响代价和计划提取；节点创建时的 guidance 不会因为 rewire 反向传播，从而避免跨 DFS 分支泄漏临时寄存决定。

## 14. Macro rollout：把一串已验证的小步包装成优先边

即使 guidance 很好，外层每次只前进一步仍可能把大量时间花在重复的明显移动上。Macro rollout 在一个节点第一次展开时，从“无动作约束”的根生成器开始，连续执行若干个贪心原子步。每一步仍然经过 `apply_ops`，所以 macro 不是瞬移。

rollout 在以下事件之一发生时停止：

```text
出现 LIFT 或 DROP 事件
到达完整目标状态
局部状态形成循环
PIBT / 验证器无法继续
达到最大步数
```

若 rollout 至少走了两步，并且终点状态还没有出现过，算法把终点作为一个额外外层后继，插到 DFS 更早尝试的位置。macro 边保存所有中间机器人状态、货架状态和真实累计代价；输出计划时再把中间步骤完整展开，所以外部验证器看到的仍是一串普通 `WAIT/MOVE/LIFT/DROP`。

Macro 不替换当前节点原有的单步约束树：

```text
原来的原子后继：仍在
额外的 macro 后继：优先尝试
```

因此单看一次搜索，macro 只增加边，不删除边。问题在于贪心 rollout 很适合抢首解，却可能连续选择局部便宜、全局昂贵的搬运，导致几百货架时方案成本显著升高。最终实现使用 D14 两阶段策略。

## 15. D14 两阶段：第一阶段抢上界，第二阶段从根重新优化

第一阶段在目标规模不超过阈值时允许 macro，超过阈值时使用纯原子动作；无论哪种情况，找到第一个完整可执行计划后都立即停止。记这个计划的真实动作成本为 U：

```text
U = phase_1_plan_cost
```

第二阶段丢弃第一阶段的搜索树，从初始物理状态重新开始，只使用原子一步后继，不使用 macro。它保留 U 作为“当前已经有这么好的方案”的上界，并立即使用 FOCAL。若一个节点满足：

```text
g + lower_bound_h >= U
```

那么从该节点继续得到的任何计划都不可能严格优于 U，可以安全剪掉。安全性来自 `lower_bound_h` 不高估，而不是来自 macro 的质量。

这两阶段的关系可以概括为：

```text
阶段 1：
目标数不超过阈值：macro-on + DFS
目标数超过阈值：primitive-only + DFS
-> 尽快找到完整可执行计划
-> 得到上界 U

阶段 2：
从根重启 + primitive-only + FOCAL
-> 不受阶段 1 贪心轨迹约束
-> 用 f >= U 剪枝
-> 尝试找到成本小于 U 的方案

最终：
返回两个阶段中成本更低的完整计划
```

如果第一阶段没有找到计划，第二阶段就没有 U，仍可从根做原子搜索。默认 macro 只在目标规模不超过阈值时启用；超过阈值时第一阶段也会退化为原子首解搜索。实际两个阶段共享总 deadline，所以第一阶段若耗尽时间，第二阶段可能没有运行机会；这属于资源限制，不改变无界搜索下的理论结论。

## 16. 一条完整因果链

到这里，可以把整个算法压缩成一条不丢关键环节的因果链：

```text
直接枚举所有机器人联合动作约为 6^R，太贵
-> 用 guidance 判断该服务谁、该清谁、该停哪里
-> 用匈牙利算法把请求分给空闲机器人
-> 用角色优先级和 PIBT 快速猜一个联合动作
-> PIBT 可能贪心失败，也可能建议得不好
-> 当前物理节点的惰性约束树继续展开
-> 逐步固定更多机器人的完整动作
-> 完整动作叶子由 apply_ops 独立验证
-> 合法叶子成为外层物理状态图的一步边
-> 外层搜索、去重、FOCAL 和 rewire 组合出多步计划
-> macro 只额外插入一串已验证步骤的终点
-> D14 在规模允许时用 macro 抢首解，再用原子搜索从根改进
```

三句话记忆版是：

```text
guidance 和 PIBT 负责“尽快猜对”。
惰性约束树负责“最终不漏”。
apply_ops 负责“每一步真的能执行”。
```

## 17. 正确性到底保证了什么

Carrier-LaCAM 的核心保证是边级可执行性：外层搜索接纳一个载具后继前，都会从机器人预约组装出完整联合动作，并交给 `apply_ops`。货架位置不是另一个规划器提供的愿望，而是验证器根据机器人动作计算出的确定结果。Macro 的每个内部步骤也走同一验证流程。

局部完备性由“完整动作候选 + 惰性约束树 + G1 + 权威验证器”给出；全局可行性完备还依赖外层最终公平地展开有限状态图，并假设没有 deadline 和内存上限。guidance、匹配、寄存和活锁机制默认只排序，不参与硬删除。

实现不主张以下更强结论：

```text
不保证给定 10 秒内一定找到存在的解。
不保证首解质量好。
不保证 anytime 搜索最终收敛到全局最优。
不把 makespan 等同于物理动作成本。
```

工程上使用三层一致性检查。第一层是生成器的内联预约规则，用于提速；第二层是 C++ `apply_ops`，是搜索边的最终裁判；第三层是独立 Python validator，对输出计划逐步重放。G1 穷举测试比较“约束树枚举结果”和“直接验证器枚举结果”，跨语言 golden 用例再比较 C++ 与 Python 对相同轨迹的判断。

## 18. 和已有方法相比，建模差异在哪里

BR-LaCAM 主要在货架层规划，把货架当成会自己走的 agent，再假设机器人能跟上执行；其保守 no-following 规则还禁止进入本步刚腾出的格子。MAPF-DECOMP 先给货架规划完整轨迹，再切段给机器人执行，货架规划时不知道机器人状态。CREST 仍保留阶段分解，只在执行期修补。NAT-CBS 真正联合搜索并可做最优规划，但联合冲突树的成本让它主要适合小规模实例。

Carrier-LaCAM 的不同不是多加一个启发式，而是换了搜索对象：

```text
分解式视角：
先有货架计划 + 再找机器人执行

Carrier-LaCAM：
只搜索机器人可执行的物理状态转移
货架轨迹 = 机器人轨迹经过 carrying 关系后的投影
```

这让“可执行性”不再是计划完成后的检查项，而成为每条搜索边的定义。同时，它保留 LaCAM 的惰性联合动作枚举和 PIBT 快速补全，因此没有直接退回 NAT-CBS 式的小规模联合最优搜索。

## 19. 实验结果应该怎样读

统一协议包含 164 个实例、7 个方法、同机同种子、每例 10 秒。Carrier-LaCAM 解出 162/164；共享贪心 rollout 核心的 B0 解出 154；单机器人顺序基线 B4 解出 115；CREST 解出 80，其执行修补变体为 38；冻结货架计划的 B1 为 74；NAT-CBS 只在 150 格以内的小图上解出 21。

只在双方都解出的实例上比较质量时，R2R 场景的平均 makespan 为 564 对旧版 548，约差 3%；S2W 为 1272 对 1278，基本持平；标准密集集比例为 1.00，并多解出一个旧版失败的 `24 x 24` 难例。小实例首解通常是毫秒级，论文级实例约 3 到 9 秒。

这些结果支持的是“联合可执行搜索可以保持大规模吞吐”，不是“所有场景质量都已经解决”。DnE 场景有 403 个目标，是当前最难的一族，质量比旧版差约 12%，超过项目预先设定的 `+5%` 红线，仍作为未豁免问题保留。B1 从历史 77 降到 74，但它本来就是刻意冻结计划的弱对照；rollout 尚未接入增量哈希，也仍有吞吐优化空间。

## 20. 工程实现为什么算“增量扩展”

当前仓库只有一个主搜索循环：`TAPFPlanner::solve`。外层节点仍是 LaCAM-TAPF 原来的 `TAPFNode` 和 `Config`，只是旁边增加了 `ShelfState`。有普通 TAPF 任务的 agent 继续走原候选和 PIBT 逻辑；没有普通任务、承担搬运职责的 carrier agent 才进入载具分支。

无货架实例不是通过“检测后切回旧算法”的开关兼容，而是通过空数据结构自然退化：

```text
ShelfState 的三个向量为空
-> 不追加 LIFT / DROP 候选
-> guidance 立即返回
-> 货架哈希贡献为 0
-> 载具代价循环为空
-> apply_carrier_effects 只复制空层
-> 原任务 agent 的候选、随机数消耗和 PIBT 路径保持原样
```

这条不变量用改造前录制的多实例、多种子 golden 序列保护，不只比较“最后有没有解”，还比较路径哈希、成本和搜索迭代行为。它曾抓出一个真实递归 bug：PIBT 候选缓冲若被所有机器人共享，优先级继承递归会在外层循环尚未结束时覆盖缓冲；当前实现改成每个 agent 独立候选数组。

另一个工程陷阱来自缓存键。临时 rollout 探针节点释放后，堆分配器可能把同一地址交给新节点；若占用缓存只拿指针地址当身份，就会把“新节点同地址”误当成“旧节点未变化”。这不是越界，所以 ASan 不会报警，却会造成跨进程 makespan 巨幅漂移。当前实现每个 rollout 步都显式作废地址键缓存，并在探针结束后再次作废。

## 附录 A：默认参数与它们的作用

这些数值是实现默认值，不是正确性定理的必要常数。除硬约束消融外，修改它们主要改变搜索顺序和速度。

| 参数或机制 | 默认值 | 含义 |
| --- | ---: | --- |
| `blocking_penalty` | 8 | Dijkstra 进入占用格的额外代价，占用格总成本为 9 |
| `clear_chain_k` | 3 | 每个目标路径最多发出的 clear 请求数 |
| `eta` | 2 | 保持上一状态同一机器人-请求配对的迟滞奖励 |
| `livelock_window` | 24 | `progress_score` 连续不改善后触发多样化的窗口 |
| duplicate reguide interval | 8 | 同一状态每重访 8 次重建 guidance |
| active target cap | 64 | 未完成目标超过 256 时，先保留所有正在搬运的目标，再补充其他目标直到通常达到 64；若在途目标已超过 64，活跃集合也会超过 64 |
| Hungarian target threshold | 256 | 目标规模不超过该值时默认使用匈牙利匹配，否则贪心 |
| `focal_weight` | 1.5 | FOCAL 接受 `f <= 1.5 * f_min` 的节点 |
| macro step cap | 64 | 一次 rollout 最多执行的原子步数 |
| macro target threshold | 64 | 默认只在目标数不超过 64 时启用 macro |
| rollout guidance refresh | 8 | rollout 内每 8 步重建一次 guidance |
| idle corridor avoidance | 开 | 空闲机器人站在保护通道上时优先走开 |
| head-on yield | 开 | 两目标载具迎面时让较远者寄存 |
| escape-degree parking tie-break | 关 | 默认不使用“出口更多更好”的停车评分 |
| strict path invalidation | 关 | 默认使用路径上的惰性非对称失效检查 |
| no-following | 关 | 默认允许进入本步刚腾出的格子 |
| `alpha,beta,gamma,delta` | 全部为 1 | 载货移动、空车移动、举放、匿名载货附加成本 |

## 附录 B：缓存为什么不影响正确性

地面距离在无墙地图上直接使用 Manhattan 距离；有墙时按目标格惰性缓存 BFS 距离。每个目标到自己终点的距离场也按需缓存。它们计算的是固定墙图上的精确距离，不随机器人和货架变化。

最少阻挡路径缓存会受上层占用变化影响，因此保存路径及占用快照。默认模式只在旧路径出现新增阻挡时重算，是有意的非对称策略：阻挡消失后旧路径仍可执行，只可能不再是最优 guidance。严格模式则比较完整占用快照。由于这条路径只排序动作，不充当硬合法性条件，即使缓存暂时陈旧也不会删除约束树中的合法后继。

搜索去重使用完整状态相等比较，哈希只负责加速桶查找；哈希碰撞不会把不相等状态合并。匿名落地占用排序和身份消除保证等价状态有规范表示。rollout 的局部循环检查目前仍会每步重新计算状态哈希，尚未使用已有的增量更新，因此还有减少全量哈希开销的性能机会。

## 附录 C：源码阅读地图

建议按下面顺序阅读，而不是一上来钻进 `solve()`：

| 文件 | 先看什么 |
| --- | --- |
| `lacam/include/dd_carrier.hpp` | `Op`、`PhysConfig`、`kappa` 编码和验证器接口 |
| `lacam/src/dd_carrier.cpp` | `initial_phys_config`、`is_dd_goal`、`apply_ops` |
| `lacam/include/tapf_planner.hpp` | `ShelfState`、`CarrierGuidance`、`TAPFNode` |
| `lacam/include/search_kernel.hpp` | 惰性约束树展开、FOCAL、LaCAM 节点骨架 |
| `lacam/src/carrier_guidance.hpp` | 最少阻挡路径、请求、匹配、寄存、停车、wait-for 图 |
| `lacam/src/tapf_planner.cpp` | 唯一 `solve` 循环、G1、PIBT 载具分支、rewire、macro |
| `lacam/src/dd_planner.cpp` | D14 两阶段封装和两个阶段的时间/上界传递 |
| `benchmark/ddbench/validator.py` | 独立 Python 计划重放验证器 |
| `tests/test_dd_carrier.cpp` | 原子动作和两层物理规则 |
| `tests/test_dd_g1.cpp` | 完整动作穷举与 G1 一致性 |
| `tests/test_dd_integration.cpp` | guidance、rollout、集成语义和缓存回归 |
| `tests/test_dd_anytime.cpp` | 上界剪枝和两阶段 anytime 行为 |
| `tests/test_dd_rewire.cpp` | 重复状态的 g 松弛与父边重接 |
| `tests/test_dd_waitfor.cpp` | 跨层等待环检测 |
| `tests/test_dd_oscillation.cpp` | 迟滞、禁忌和活锁多样化 |

本次代码核对中，核心测试结果为：`test_dd_carrier` 12 项通过，`test_dd_g1` 6 项通过，`test_dd_anytime` 4 项通过，`test_dd_integration` 14 项通过。

## 结语

Carrier-LaCAM 最值得记住的不是某个权重或某条停车规则，而是它对“谁有动作”的回答：**机器人是唯一执行器，货架移动只是机器人动作在上层的确定性后果。**

这个建模把机器人路径和货架路径变成同一条物理状态轨迹的两个投影。LaCAM 的 PIBT 和惰性约束树因此还能继续负责大规模联合动作生成，`apply_ops` 则把可执行性固定在每一条边上。最终得到的不是“货架计划加机器人补丁”，而是一条从第一步起就能被完整重放的仓库物理过程。
