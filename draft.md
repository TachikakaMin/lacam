现在我们需要把现在的one-shot TAPF改成lifelong TAPF，你先阅读项目代码。

我们地图先用symbotic.map，在这个map中@代表不能通过的墙，其他地方代表可以走动的地方

里面的a代表货架通道，o代表outbound通道，i代表inbound通道

我们现在生成task包括两个部分，如果是outbound，需要生成一个起始点在a，终止点是一个set，这个set里面所有点需要在o上，set大小设置为目前设置为5. 如果是inbound，需要生成一个起始点在i，终止点是一个set，这个set里面所有点需要在a上，set大小设置为目前设置为5. 所有点随机生成，但未完成的任务之间起始点不能重复，每个10个step生成一个新任务，但在timestep 0的一开始生成agent数量个task。 

我们的lifelong lacam-tapf改造如下：现在我们的lacam-tapf算法是针对tapf问题，即一个agent有多个goal loc，只需要去一个即可。针对我们的新任务，我们假设agent存在两种状态，一种是unloaded，另一种是loaded，当agent处于unloaded的时候，agent可以选择去任意一个任务的起始点，当agent处于loaded的时候，agent可以选择去这个任务的终止点set里面任意一个终止点，所以这么看来，lifelong依旧是tapf形式，我们依旧有一个cost matrix去跑task assignment. 但问题在于task assignment的cost，对于loaded agent，此时的cost和原本lacam-tapf没有区别，对于unloaded agent，我们需要考虑整个任务的cost，所以此时到任何一个task的cost等于这个agent到任务起始点加上这个任务起始点到它的goal set中最近goal的cost，不考虑冲突。

预处理：你需要先对地图进行预处理，算出所有可通过点到可通过点的最短路距离，并保存成可以快速读取的文件，用于之后算法的planning。

我们现在的任务：
1. 写一个对应的生成任务的程序，并且测试。
2. 完成我们lifelong lacam-tapf算法的改造，并且测试。
3. 测试我们lifelong lacam-tapf算法在当前symbotic上的throughput，测试不同agent数量，从，总timestep数为1000,并记录对应metric，每次invoke算法plan时长控制在2s以内

所有实验优先跑smoke test测试，smoke test不应该超过10s。